//Apresentar a coleção Set
// Explicar sua aplicação
// Explicar a diferença entre set e array
//sets  são estrutas que armazena valores únicos

// Estrutura 
const mySet = new set();

// Metodos 
mySet.add(1);
mySet.add(6); //  adicionar 

mySet.has(4);
//ver se existe um valor // true

mySet.delete(4);
//para deletar

/*Set vs arrays
*Possui valores unicos;
*em vez da propriedade length, consulta se o numero de registros propriedade size
*não possui os metodo map, filter, reduce, etc
set e mais limitado*/
